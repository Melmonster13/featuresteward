version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries:
      - internal/store/queries.sql
      - internal/store/auth_queries.sql
    gen:
      go:
        package: db
        out: internal/store/db
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
